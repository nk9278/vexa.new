# Installation Guide

1. Clone or upload the repository to your hosting environment.
2. Import `database.sql` to your MySQL database.
3. Update `config/constants.php` with your database credentials and `BASE_URL`.
4. Ensure the server supports PHP 8+ and MySQL (PDO).
5. Access the application via the configured `BASE_URL`.
